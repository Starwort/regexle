import {createTheme} from "@suid/material";

export const dark = createTheme({
    palette: {
        mode: "dark",
    },
});
export const light = createTheme({
    palette: {
        mode: "light",
    },
});